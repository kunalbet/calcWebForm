import { OperandValidatorDirective } from './operand-validator.directive';

describe('OperandValidatorDirective', () => {
  it('should create an instance', () => {
    const directive = new OperandValidatorDirective();
    expect(directive).toBeTruthy();
  });
});
